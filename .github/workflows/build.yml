name: build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:  

jobs:
  semgrep:
    name: Scan
    runs-on: ubuntu-latest
    container:
      image: returntocorp/semgrep-action@fcd5ab7459e8d91cb1777481980d1b18b4fc6735
    steps:
      - name: FetchSource
        uses: actions/checkout@v4
      - name: Semgrep
        run: semgrep ci --config=auto --sarif --output=semgrep.sarif || true
      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: semgrep.sarif
        if: always()
